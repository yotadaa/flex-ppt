# QA Artifacts

This folder keeps intentional validation artifacts for Flex-PPT.

- `screenshots/` stores curated screenshots that are useful for visual regression and handoff context.
- Temporary browser profiles, scratch captures, and runtime output remain ignored by `.gitignore`.

